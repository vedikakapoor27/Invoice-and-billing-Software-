#imp things to keep in consideration

1:Mapstruct for mapping entity object to DTO object.
2:Lombok is a very useful library. This library takes care of setter/getter generation and constructor. We don’t have to write manually getter/setter, Constructor.